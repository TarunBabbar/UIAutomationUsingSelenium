using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace UIAutomationUsingSelenium
{
    [TestClass]
    public class TestAssemblyInitialize
    {
        // Static WebDriver and LoginPage removed to support parallel test execution.
        // Assembly-level setup/cleanup is not needed for per-test WebDriver usage.
    }
}
